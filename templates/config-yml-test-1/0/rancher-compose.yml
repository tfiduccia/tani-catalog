version: '2'
services:
  .catalog:
    name: "Services with advanced options"
    version: "1.0"
    description: "Services"
    questions:
      - variable: "TEST_STRING"
        label: "String"
        required: false
        default: "hello"
        type: "string"
  config-test:
    scale: 1
